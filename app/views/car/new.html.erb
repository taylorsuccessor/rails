<%= form_tag("/car", method: "post") do %>
  <%= label_tag(:name, "Search for:") %>
  <%= text_field_tag(:name) %>
  <%= submit_tag("Save") %>
<% end %>


<%
if @car && @car.errors.has_key?(:name)
%>
<%=@car.errors[:name].first() %>
<% end %>

<%
if @car && @car.errors.any?
  @car.errors.each do |attribute, message|
%>
<%= attribute %>
<%=message %>
<%
  end
  end
  %>